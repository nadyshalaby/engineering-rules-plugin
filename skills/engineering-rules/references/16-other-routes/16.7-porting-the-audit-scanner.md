# 16.7 [reference] Porting the audit scanner

## Porting the scanner (on-demand deterministic audit for a non-JS stack)

When the project has no deterministic scanner of its own, generate a throwaway scanner for
its primary language in-session, run it, and delete it. This contract holds for every
language, the ECMAScript family included: a TS/JS project with no scanner gets one generated
the same way. The goal is one deterministic core for every stack, grounded in the same
concepts and the same output shape, never invented fresh each time.

Mirror this four-part structure: a lexer/masker, the check set, the exemption table and the
driver that walks the tree. Only the language-specific parts change between languages: the
masker's comment and string syntax, and the rule analogs.

### Hard contract (non-negotiable)

1. **Ephemeral.** Write the generated script under a fresh `mktemp -d`. Never write it into the
   audited project. Delete the temp dir in Phase 6.
2. **Same output schema.** Emit one JSON object in this shape so Phase 4 merges it uniformly:
   ```json
   {"schema_version": 1, "root": "...",
    "config": {"...": "...", "path_list_supplied": false, "scoped_paths": 0,
               "listed_lines": 0},
    "stats": {"files_scanned": N, "unread_too_large": 0, "unread_unreadable": 0,
              "paths_outside_root": 0, "paths_in_skipped_dir": 0, "paths_not_found": 0,
              "paths_unsupported": 0, "paths_unaccounted": 0, "lines_blank": 0,
              "lines_comment": 0, "lines_duplicate": 0, "lines_malformed": 0,
              "lines_unaccounted": 0, "findings": K},
    "findings": [{"rule_id": "...", "category": "...", "severity": "...",
                  "confidence": "exact", "file": "rel/path", "line": 12, "end_line": 12,
                  "message": "...", "snippet": "...", "fixable": "manual"}]}
   ```
   The drop buckets and their two `*_unaccounted` subtractions are part of the shape, not optional
   trim. **Inputs are lost in two separate places, and a third family says what was located and
   never read.** At the scan stage, every path has to leave the iterator through exactly one
   counter, so `files_scanned +` the `unread_*` buckets `+` the four `paths_*` buckets must equal
   `config.scoped_paths`, and `paths_unaccounted` publishes that subtraction. At the parse stage,
   before any of that runs, the path LIST loses blank lines, comment lines, duplicates and
   malformed lines (a bare `.` or `./` carries no path), so `config.scoped_paths +` the four
   `lines_*` buckets must equal `config.listed_lines`, and `lines_unaccounted` publishes that one.
   A generated scanner that omits either family can under-cover a scan and still report clean.

   **Three families, and they must not share a name prefix**, because consumers sum drop buckets by
   prefix. `unread_*` is separate for that exact reason: it counts files that were located, were in
   scope, and had no rule run against them (past the size ceiling, or undecodable), and folding
   them under `paths_*` puts them straight into every caller's "covered" total. That is not a
   hypothetical, it is how a readable 2.3MB file 400x over the line cap once reported as a clean,
   perfectly balanced scan. A `paths_*` drop can be normal (the diff deleted the file, the
   extension is unsupported); a non-zero `unread_*` never is.

   **And a subtraction is not enough on its own.** This is the PARSE stage's problem specifically.
   `lines_unaccounted` reads 0 for a list carrying a blank, a duplicate and a comment, because
   those drops are bucketed and the arithmetic balances perfectly while three of four inputs
   vanish. A reconcile that subtracts known buckets proves nothing about whether those buckets
   should have been hit at all, and in a `--name-only` dump none of them should. A caller that
   needs its exact list scanned must compare `config.listed_lines` against `config.scoped_paths`
   directly, which is the equality check every caller makes. The SCAN stage is the other way
   round: `paths_not_found` and `paths_unsupported` are expected on a real diff, so there the
   subtraction, plus a non-zero `unread_*`, is what you read.

   **Scoping is the flag, not the list length.** `path_list_supplied` records whether the paths
   flag was passed at all. A whole-tree run has no path list, so every `paths_*` and `lines_*`
   bucket reads 0 and `scoped_paths` reads 0; a scoped run handed an EMPTY list also reads 0 and
   must scan nothing. Branch on the flag, never on the emptiness of the list, or a zero-path scope
   silently becomes a whole-tree walk. `unread_*` still counts in both modes.
3. **False-positive discipline.** Mask comments and string literals before matching code-construct
   bans; a ban hiding in a string or comment must NOT match. Only
   emit `confidence: exact` for checks that are truly exact; mark anything heuristic as such.
4. **Same carve-outs.** Apply the path-exemption model in 16.5: skip dependency/
   build dirs, generated files, and migrations; waive the inline-type, non-null, bare-error and
   hygiene-marker rules in test files, keep the suppression ban there with 1.1's sole
   exception, and run `test.focused` / `test.skipped` ONLY there; apply the inline-type analog
   only to that language's equivalent of scoped modules and the `console`-style debug-artifact
   analog only to its domain modules.
5. **Read the project's bans.** Load the project's ban-patterns file (translate POSIX classes
   to the host regex engine) so project-defined bans are honored, not duplicated.
6. **Obey the caps it enforces.** The generated script itself stays within ≤500 lines/file,
   ≤40 lines/function, ≤3 params, ≤3 nesting.
7. **Same flags.** `--root <dir>` (the project root, resolved relative to the project, never an
   absolute user path in the report); `--paths <file>` (a newline-separated path list; its
   presence sets `path_list_supplied`); `--text-only-ext <ext>` (repeatable; files with that
   extension get ONLY the file-line cap and the project ban patterns); `--ban-patterns <file>`
   (the project's ban-patterns file, when it has one). JSON goes to stdout, diagnostics to
   stderr, exit code 0 on a completed scan whatever it found and non-zero only when the scan
   itself could not complete.

### Rule analogs by language

Keep the universal checks (file-line cap; secret regexes, those are language-independent).
Translate the construct bans to each language's real syntax. Drop a row where the language has
no analog (e.g. Python has no non-null `!`).

| rule_id | TS/JS | Python | Go | Rust | Java/C# |
|---|---|---|---|---|---|
| `cap.file-lines` | line count | line count | line count | line count | line count |
| `ban.suppression` | `@ts-ignore`, `@ts-expect-error`, `@ts-nocheck`, `biome-ignore`, `eslint-disable`, `oxlint-disable`, `deno-lint-ignore`, `tslint:disable`, `stylelint-disable`, `$FlowFixMe`, `NOSONAR`, `noinspection`, the coverage markers `istanbul ignore` / `c8 ignore` / `v8 ignore` / `node:coverage ignore` | `# type: ignore`, `# noqa`, `# pyright: ignore`, `# pylint: disable`, `# mypy: ignore-errors`, `# nosec`, `# pragma: no cover` | `//nolint`, `//lint:ignore`, `// #nosec` | `#[allow(...)]`, `#![allow(...)]`, `#[expect(...)]` | `@SuppressWarnings`, `@SuppressLint`, `[SuppressMessage]`, `#pragma warning disable`, `#nullable disable`, `// NOSONAR`, `// NOPMD`, `// CHECKSTYLE:OFF` |
| `ban.empty-catch` | `catch {}`, `catch { /* … */ }`, a `}` on the next line, `.catch(() => {})` | `except: pass`, `except X: ...`, `with suppress(X):` | `_ = err`, an empty `if err != nil {}`, a dropped error return (judgment) | `let _ = fallible()`, `.ok();` on a `Result` (judgment) | `catch (...) {}`, `catch { }` |
| `ban.bare-error` | `throw new Error(`, `throw Error(`, `throw "text"`, `reject(new Error(` | `raise Exception(`, `raise BaseException(`, `raise RuntimeError(`; `ValueError`/`TypeError` are judgment | `errors.New(`/`fmt.Errorf(` inside a function (a package-level sentinel is fine) | `panic!(` in library code (judgment) | `throw new Exception(`, `RuntimeException(`, `Throwable(`, `SystemException(`, `InvalidOperationException(` |
| `ban.non-null` | `x!` (not `!=`, not a definite-assignment `name!: T`) | n/a | n/a | `.unwrap()`, `.expect(` (judgment; fine in tests and in `main`) | C#'s null-forgiving `x!` (judgment) |
| `ban.inline-type` | `interface`/`type`, or a 2+ member object type literal in a signature, in a scoped module | dataclass/TypedDict/BaseModel in a router/service module | a struct type declared inside a handler file | a struct declared in a handler module | a nested class in a controller |
| `clean.debug-artifact` | `debugger`; `console.*` in domain code | `breakpoint()`, `pdb.set_trace()`; `print(` in a module that is not a CLI entry | `fmt.Println(` in domain code | `dbg!(`, `println!(` in library code | `System.out.println(`, `Console.WriteLine(` in domain code |
| `test.focused` | `.only(`, `fit(`, `fdescribe(` | n/a (a `-k` on the command line is not committed) | n/a | n/a | n/a |
| `test.skipped` | `.skip(`, `.todo(`, `xit(`, `xdescribe(` with no ticket after it | `@pytest.mark.skip`, `pytest.skip(`, `@unittest.skip` with no ticket | `t.Skip(`, `t.SkipNow(` with no ticket | `#[ignore]` with no ticket | `@Ignore`, `@Disabled` (JUnit), `[Ignore]` (NUnit, MSTest) with no ticket |
| `sec.hardcoded-secret` | universal regexes | universal | universal | universal | universal |

The same rows for five more stacks. "Judgment" marks an analog a grep can find but only a
reader can confirm, the same way `ban.bare-error` works in every language.

| rule_id | PHP | Ruby | Kotlin | Swift | Dart |
|---|---|---|---|---|---|
| `ban.suppression` | `@phpstan-ignore-line`, `@phpstan-ignore-next-line`, `@psalm-suppress`, `phpcs:ignore`, `phpcs:disable`; the `@` error-suppression operator (judgment, it needs a lexer) | `# rubocop:disable`, `# rubocop:todo`, `# standard:disable`; Sorbet's `T.unsafe` (judgment) | `@Suppress("…")`, `@file:Suppress(…)`, `// ktlint-disable` | `// swiftlint:disable`, `// swiftlint:disable:next` | `// ignore: rule`, `// ignore_for_file: rule` |
| `ban.empty-catch` | `catch (\Throwable $e) {}` | `rescue nil`, `rescue => e` followed by `end`, `rescue StandardError; end` | `catch (e: Exception) {}`; `runCatching { }.getOrNull()` (judgment) | `catch {}`; `try? x()` that drops the error (judgment) | `catch (e) {}`, `on X catch (_) {}` |
| `ban.bare-error` | `throw new \Exception(`, `throw new Exception(`, `throw new \RuntimeException(` | `raise "text"`, `raise StandardError`, `raise RuntimeError` | `throw Exception(`, `throw RuntimeException(`, `throw IllegalStateException(`; `error("…")` (judgment) | `throw NSError(`; `fatalError(` (judgment) | `throw Exception(`, `throw Error(`, `throw "text"` |
| `ban.non-null` | n/a | n/a (Sorbet's `T.must` is judgment) | `x!!` | `x!` force unwrap, `try!`, `as!` | `x!` |
| `ban.inline-type` | an inline array-shape docblock in a controller (judgment) | a `Struct.new` in a controller (judgment) | a `data class` declared in a controller or service file | a `struct` declared in a view-controller file | a `class` declared in a widget file (judgment) |
| `clean.debug-artifact` | `var_dump(`, `print_r(`, `dd(`, `dump(`; `error_log(` in domain code | `binding.pry`, `byebug`, `debugger`; `puts`/`p` in domain code | `println(` in domain code | `print(`, `debugPrint(` in domain code | `print(` in domain code; `debugger()` |
| `test.focused` | n/a | RSpec `fit`, `fdescribe`, `focus: true` | n/a | n/a | `solo: true` |
| `test.skipped` | `markTestSkipped(` with no ticket | `skip "…"`, `xit`, `pending` with no ticket | `@Ignore`, `@Disabled` with no ticket | `XCTSkip(`, `throw XCTSkip(` with no ticket | `skip: true`, `skip: '…'` with no ticket |

Severity and category come from 16.4; keep them identical so the merged report is
consistent across the deterministic and on-demand engines. The law scout (9.5) already
greps the suppression, empty-catch, bare-error, debug-artifact and skipped-test analogs from
both tables on every path, and the non-null `!` row on the files whose language has the
operator; a generated scanner adds the masker so a token inside a string or a comment stops
counting.

### Masking notes per language

- **Python**, comments `#…` to end of line; strings `'…'`, `"…"`, and triple-quoted
  `'''…'''` / `"""…"""` (multi-line, track across lines like the JS template state); raw/f
  strings prefix-aware. No braces, so there is no brace-based structure check, leave function
  length / nesting to the linter or semantic pass.
- **Go / Rust / Java / C#**, `//` line and `/* */` block comments; `"…"` strings;
  Go/C# raw strings (backtick / `@"…"`); Rust raw strings `r#"…"#`. Brace-based, so the JS
  masker's logic transfers almost directly, only the comment/string token set changes.
- **PHP / Ruby / Kotlin / Swift / Dart**, PHP has `#`, `//` and `/* */` comments and
  heredoc/nowdoc strings; Ruby has `#` comments, `=begin`/`=end` blocks, `%q()` strings and
  heredocs; Kotlin and Dart are C-style with triple-quoted multi-line strings; Swift nests its
  `/* /* */ */` block comments, which a flat masker must count rather than match.

When in doubt, prefer a missed finding (false negative) over a false positive: an on-demand
scanner that cries wolf is worse than one that quietly defers a class of checks to the semantic
pass. State in the report which deterministic checks the on-demand scanner actually ran.
