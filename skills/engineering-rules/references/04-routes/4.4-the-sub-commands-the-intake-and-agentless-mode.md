# 4.4 [route] The sub-commands, the intake and agentless mode

The route table in `SKILL.md` names nine routes. Each one has a sub-command of its own, the
bare invocation runs a guided intake, and every route runs agentless unless the user switches
helpers on. This file is
the whole of that contract. `SKILL.md` "Invocation" points here, and the always-on part of
agentless mode is in 1.8, so that none of it depends on having reached this file.

## The sub-commands

| Sub-command | Route | Section | What follows it |
|---|---|---|---|
| `/engineering-rules:quick` | Quick mode | 4.2 | the request |
| `/engineering-rules:full` | Full mode | 4.3 | the request |
| `/engineering-rules:shape` | Shaping | 16.1 | the idea |
| `/engineering-rules:explore-feature` | Explore a feature | 16.9 | the entry point |
| `/engineering-rules:audit` | Codebase audit | 16.3 | a project root or directory; nothing means the current project |
| `/engineering-rules:triage` | Review triage | 16.2 | the pasted findings |
| `/engineering-rules:skill` | Skill authoring | 16.12 | what the skill should do |
| `/engineering-rules:design` | Design spec | 15.19 | the mode or a direction; nothing means detect it |
| `/engineering-rules:log` | Update log | 16.13 | nothing |

Each is a skill of this plugin, `skills/<route>/SKILL.md`, and only the user can invoke it:
the model cannot call one, so a route reached through a sub-command was picked by the user
and nobody else. Its body does three things and nothing more. It loads this skill, through
the Skill tool with `engineering-rules:engineering-rules`, so the always-on law binds. It
reads the request that follows. Then it hands over to the route's own section. A sub-command picks the row and skips the classification in 4.1. It waives nothing:
not a phase, not a check, not a gate, not the question contract. Quick mode named by
`/engineering-rules:quick` is quick mode named by the user, which is the one way quick mode
ever starts (4.2).

**The tenth sub-command, `/engineering-rules:helpers`.** It picks no route; agentless is the
default helper mode (below), and this switches helpers on for the task in flight and for
every task that starts in this conversation, until `/engineering-rules:helpers off` or the
user's words switch back. With a request after it, the request classifies under 4.1 as prose
typed into `/engineering-rules:engineering-rules` does, full mode unless the words name
another route, and starts with helpers. Bare with a task in flight, that task switches from
its next run point, helper line printed after one probe. Bare with none, helpers are on, said
in one line, and nothing else starts and nothing is asked. It combines with every route
sub-command: switch first, then type the route, or name the route in the request.

**The request.** Everything after the sub-command. When that is empty, the request is the
most recent ask in this conversation, in the user's own words. When there is none either,
ask for it through the wizard tool, one question, and ask nothing else: the helper mode stays
as the conversation has it, agentless unless switched, because a bare sub-command is somebody
who already said which route they want. `audit`, `design` and `log` are complete with nothing
after them.

**Typed into the main skill.** `/engineering-rules:engineering-rules <request>` carries no
route token any more. The request classifies under 4.1 exactly as prose does: a route the
user names in their words still picks itself, and a first word that used to be a token
(`full`, `shape`) is their word too.

## The intake, when the skill is invoked bare

`/engineering-rules:engineering-rules` with nothing after it, from the chat box, is a user who
wants to be walked through the options. Three facts have to be settled before anything
starts: the route, the helper mode, and the request. Settle them through the wizard tool, in
as few calls as the answers allow, and never with a question the last answer did not make
necessary. The wizard holds four options per question, so the nine routes are reached in two
steps, never listed in one.

1. **What are you here for?** Four options, the first recommended.
   - *Build, fix or change something* → quick or full mode, step 2a.
   - *Think an idea through before building it* → shaping.
   - *Understand or judge code that is already there* → explore-feature or audit, step 2b.
   - *Something else* → triage, skill authoring, the design spec or the update log, step 2c.
2. **Which route**, only where step 1 left more than one.
   - **2a**: *Full mode: a written plan you approve first, a spec review, a durable work-doc*
     (recommended) / *Quick mode: every check, no written plan*.
   - **2b**: *Explore one feature, entry point to leaves, as a page* / *Audit the whole codebase against its rules*.
   - **2c**: *Respond to a batch of review findings* / *Author a new skill* / *Author or refresh
     the design spec* / *Print the update log of the task in flight*.
3. **Helpers**, in the same call as step 2 where there is one: *Agentless: everything in this
   chat* (recommended, the default) / *Send helpers for the heavy reading and the fresh-eyes
   review*. Not asked for the update log, and not asked when `/engineering-rules:helpers`, or
   its `off`, already settled this conversation: that answer stands.
4. **The request**, in the words the route needs: the task, the idea, the entry point, the
   findings, the skill's job, the design mode. The audit and the log need none. Offer
   *Continue what this conversation was last discussing* when there is such a thing, else
   *Start from the repository: read its rules, README and layout, then ask me again*; and
   *Not sure yet, shape it with me*, which is shaping. The wizard's own free-text choice is
   where they type it. A request that reads as a different route than the one chosen is put
   back as one more question, never silently re-routed.

The intake ends the moment all three facts are known, and the next line is the route and mode
statement of 1.7, step 2. Nothing is classified, no ledger opens and no file is read for the
task before then. The intake never fires when this skill loaded automatically because a task
began, when the message carries a request in any form, or when a sub-command loaded it: a
menu in front of somebody who already said what they want is ceremony (4.1).

## Agentless mode

**What it is.** The default helper mode: every helper shape (5.5) runs in this session and
nothing is sent to the agent tool for the whole task. It is the collapse rule of 1.8, *where
the runtime offers no agent tool, every shape runs here*, as the standing default rather than
a fallback.

**What switches helpers on.** Any one of these, and nothing else:

- the `/engineering-rules:helpers` sub-command, bare or with a request, which holds for the
  task in flight and for every task that starts in this conversation until it is switched
  off;
- the user saying so in their own words in the message that starts the task (*"use
  helpers"*, *"with subagents"*, *"fan out agents"*, *"send a reviewer"*), or the intake's
  helper question;
- a standing line to that effect in the user-global or project `CLAUDE.md`, which is the
  user's words for every task until they change it.

Helpers are never switched on from the size of the task, the model, the runtime's load or a
send that would be quick to brief. Those are reasons to offer them in one line, never to take
them.

**What it changes.** The recorded helper line reads `Helpers: agentless, the default;
every shape runs in this session.` (1.8, 5.1), and every row of the mandatory-sends table in
1.8 is the same work done here, in the row's order, with the output the send would have
returned:

- Phase 1 composes the question batch here, after reading 6.2, 6.4, 6.5, the matched bank,
  6.12 and 6.15 from disk;
- the scouts (9.4, 9.5, and 15.29 on UI-bearing work) are read from disk and run here at
  every stage end and at Phase 5 start, staging tables and all;
- the spec review of Phase 2.5 (8.2) and the five checks of Phase 5 (12.3 to 12.7) run here,
  one lens at a time, each check read from disk as it opens and closed before the next opens,
  over a fresh `git diff` and never over memory;
- every count, grep and planted-instance proof (1.5) runs here, with its output pasted.

Each affected ledger item records `helper skipped: agentless, the default`, which 1.8 and
5.1 accept as the recorded helper line. Nothing else moves: not a check, not a scout, not a
gate, not the evidence rules, not the wizard tool, not the caps and bans.

**What it costs, said once at task start, in one line.** The fresh eyes of the Phase 5 review
and the spec review are gone: the context that wrote the diff is the one reading it. And
every reference a helper would have read in a throwaway context is read into this one, so the
session thinks in what is left. *"Agentless: the review loses its fresh eyes and the reading
lands in this context."*, or words to that effect, and then the task runs.

**Switching helpers on.** The user types `/engineering-rules:helpers`, or says so in their
own words, at any boundary. The session probes the runtime once as 1.8 says, prints
`Helpers: available, by user choice.` on its own line, and the sends resume from the next run
point; nothing already done is re-done, and the switch holds for the tasks that follow until
`/engineering-rules:helpers off` or their words. Switching back to agentless mid-task works
the same way, with the cost line printed again.

## Anti-rationalizations

| Thought | Reality |
|---|---|
| "Agentless, so the scouts and the review can be lighter." | Agentless moves the reading here; it removes nothing. Every scout, every check, every count runs in this session, in the same order, with the same tables. |
| "The task is big, I'll send helpers." | The user switches them on. Offer it in one line; do not take it. |
| "The user typed a sub-command, so the clarify round is theirs to skip." | A sub-command picks the route. Phase 1 runs in full on every route that has one. |
| "Bare invocation, I'll guess the route from the last message." | Three facts, through the wizard tool, then start. The last message is an option offered, never a guess taken. |
| "The intake needs nine options and the wizard holds four." | Two steps: what they are here for, then which route in that bucket. |
| "A fork is not really a helper, so the default still allows one." | A fork is a send. Agentless, the default, sends nothing; the file is read here. |
| "The switch was two tasks ago, surely it lapsed." | It holds for the conversation until `off` or the user's words. Record it on every task's helper line. |

## See also

- `SKILL.md` "Invocation, the three ways in", and 1.8, the always-on part of this file.
- 4.1, the classification a sub-command skips.
- 5.5, the shapes that collapse into this session under agentless.
- 5.1, the helper line and the refusal it feeds.
