# 1.8 [rule] Helpers, the standing authorization and the mandatory sends

The canonical text of this section is inlined in `SKILL.md` under **Always-on law, 1.8**,
because it binds from the moment the skill loads and a reference you have to fetch is a
reference you might not fetch. That is the whole reason this section exists: the permission
to send a helper cannot live somewhere you only look once you have already decided to send
one. This file exists so the section numbering stays stable. It carries no rule text of its
own; if anything here ever differs from `SKILL.md`, `SKILL.md` wins and this file is the
defect. The four shapes, the return contract and the runtime mapping are 5.5.
