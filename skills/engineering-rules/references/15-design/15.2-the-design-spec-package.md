# 15.2 [reference] The design-spec package

## Design Spec (package index)

The producing half of the design discipline. 15.1 is the **law** (what good looks like, what never ships); this package is the **artifact** (a committed spec the code is written against and reviewed against).

Without a spec, design intent lives in a chat message and evaporates. Every later session re-derives taste from scratch, no reviewer can check compliance, and the product drifts. The spec makes the decision durable.

---

### The pipeline

```
Phase 1  Clarify      the read and the dials declared        15.20
   ↓                  official system in play?               15.21
   ↓                  direction chosen or inherited          15.4
   ↓                  redesign? mode detected, audit run     15.28, 6.9
Phase 2  Plan         DESIGN.md named as a deliverable       15.3, 15.19
   ↓                  (existing design? → 15.5; images → 15.31)
   ↓                  composition plan for a marketing page  15.22, 15.27
Phase 3  Implement    code written against {token.ref}s      15.3, 15.26
   ↓                  copy, imagery, motion under the law    15.23, 15.25
   ↓                  design scout at every UI stage end     15.29
   ↓                  preview.html generated                 15.19 step 8
Phase 4  Verify       the design pre-flight ticked           15.30
   ↓
Phase 5  Review       scout re-run; 12.5 audits the diff     15.29, 12.5
   ↓                  against the spec, the plan, the tells  15.24
Phase 6  Finish       spec + preview ship with the change    "Output contract" below
                      update log lists assets still needed   15.23, 16.13
```

**Phase 6 writes no link back.** The spec and its preview are committed files at a fixed path, `<project>/docs/design/`, which is where a reader already looks for them; Phase 6 lands them with the rest of the change under whichever landing the user picks.

Standalone entry point: the design-spec route (15.19) authors, refreshes or extracts a spec without running a full task.

---

### Files

| Section | What | Read when |
|---|---|---|
| 15.3 The design spec contract | The binding `DESIGN.md` anatomy: frontmatter token schema, `{token.ref}` syntax, the eleven prose sections, the web/native mapping table, authoring rules, validation checklist. | Authoring, refreshing, or auditing any spec. |
| 15.4 The direction library | Twelve visual directions with palette logic, type pairing, motion character, signature move, and anti-tells. The single canonical direction list; the profiles are 15.7 to 15.18. | Phase 1, when the direction is chosen or inherited. |
| 15.5 The extract protocol | Deriving a spec from existing code, a reference site, or screenshots. Plus REFRESH mode and the merge rules. | The design already exists somewhere. |
| 15.6 and section 17 | Picking one of twelve complete, original, ready-to-drop specs. | Starting fresh, or needing a worked example of the contract. |
| 15.19 Authoring or refreshing the spec | The route that produces `DESIGN.md` and `preview.html`. | Any time a spec is authored, extracted, refreshed or validated. |
| 15.20 The design read and the three dials | The six signals, the one-line read, `DESIGN_VARIANCE` / `MOTION_INTENSITY` / `VISUAL_DENSITY`, the per-direction defaults, the rotation rule. | Phase 1, before a direction is chosen. |
| 15.21 The design-system map | Brief-to-package table for the official systems, the honesty rule, how the spec records an inherited system, the Liquid Glass approximation. | The read names or implies a system. |
| 15.22 Page composition | The composition plan, hero, nav, section rhythm, bento, cards, lists, CTAs, theme lock, mobile collapse, native screens. | Any marketing surface; the "every surface" rules for product UI. |
| 15.23 Copy, imagery and assets | The copy self-audit, punctuation (the em-dash ban), names and numbers, imagery priority, logos, icons, emoji, page chrome. | Phase 3, and before Phase 4 ticks. |
| 15.24 The AI-tell catalog | Every tell with a stable `tell.<area>.<slug>` id, severity, detection hint and fix direction. | Citing an id; never end to end. |
| 15.25 Motion discipline and recipes | Motivated motion, claimed equals shown, the forbidden patterns, the sticky-stack, horizontal-pan, reveal-stagger and scroll-driven recipes. | `MOTION_INTENSITY` above 3. |
| 15.26 Frontend implementation guardrails | Dependencies, viewport units, responsive verification, hardware acceleration, dark mode, Core Web Vitals, fonts, icons, the z-index scale, semantics. | Before the first component. |
| 15.27 The pattern vocabulary | Composition axes, hero paradigms, nav, layouts, cards, scroll, galleries, type, micro-interactions, native patterns, the block library. | Naming a pattern in the plan. |
| 15.28 The redesign protocol | Mode detection, the audit, the preservation rules, the levers, the decision tree, what never changes silently, the category audit. | Any redesign, revamp or polish. |
| 15.29 The design scout | The deterministic tell finder: runnable block, rows and guards, staging table, triage. | Every stage end that wrote a UI file; Phase 5 start. |
| 15.30 The design pre-flight | The full checklist, mechanical boxes pointed at the scout, judged boxes with their evidence. | Before Phase 4 ticks; inside 12.5. |
| 15.31 Image-first work and brand direction | One image per section, never crop, the deep-analysis checklist, anti-drift, mobile screen sets, brand kits. | A generation tool exists, or the deliverable is a brand. |

Related, outside this package:

| Section | What |
|---|---|
| 15.1 | The visual law. Bans, musts, the quality gate. Sends you here. |
| 12.5 | The design-conformance check, the standing review lens on UI-bearing diffs; it re-judges the design scout's rows and re-runs the pre-flight. |
| 9.4, 9.5 | The sibling scouts; 15.29 shares their run points, path list and staging shape. |
| 6.9 | The redesign question bank that runs before the redesign protocol (15.28). |

---

### Output contract

The spec lands in the **user's** project, not in this plugin:

```
<project>/docs/design/
  DESIGN.md        the spec
  preview.html     visual catalog, light + dark toggle
```

This mirrors the existing `<project>/docs/work/` convention: committed to git, visible to humans and to other tools, not hidden in pass-only storage.

One spec per product. Never author a second spec beside an existing one; refresh it (15.5, REFRESH mode).

---

### When this package applies

Any task touching UI, styling, theming, layout, components, typography, color, spacing, icons, forms, motion, brand, RTL, responsive behavior, copy shown in a UI, imagery, or visual polish, on web **or** native.

| Situation | What happens |
|---|---|
| Project has `docs/design/DESIGN.md` | Load it. Design within it. Its tokens are binding. |
| Project has tokens but no spec | Run 15.5 Mode A, propose the recovered spec. |
| Project has neither, task is UI-bearing | Choose a direction in Phase 1, author the spec in Phase 2, before any component is written. |
| Task is a one-line copy or spacing fix | No spec needed. Honor the existing one if present. |

**The spec comes before the components.** Writing components first and extracting tokens afterwards produces a spec that documents accidents. Author the token layer, then build against it.

---

### The three rules that survive everything else

1. **One direction, committed.** Mixing directions produces a product with no point of view.
2. **Every value is a token.** A raw hex or bare pixel value in a component is the beginning of drift.
3. **The spec is the contract.** When code and spec disagree, one of them is a bug, decide which, in writing, and fix that one.
