# 15.4 [reference] The direction library

## Direction Library

Twelve visual directions, each specified deeply enough to build from. This is the **single canonical direction list**; 15.1 defers here, and every `DESIGN.md` sets its `direction:` key to one of these slugs (or `custom` with a written rationale).

Pick ONE. Commit. The core law is unchanged: *a strong, coherent aesthetic with a few bold choices beats safe-average UI.* A direction is not decoration, it is the argument the interface makes about what matters.

**How to choose.** Read the product's job first, then the audience's state of mind. A tool people live inside all day wants a direction that gets quieter over time (`industrial-precision`, `nordic-calm`, `data-dense`). A surface people meet once wants one that lands immediately (`editorial-print`, `brutalist-mono`, `playful-pop`). When the user names a mood, map it here rather than inventing a thirteenth direction.

**The read comes first.** Declare the one-line read and the dials (15.20) before opening the table below; its vibe words map here. When the brief names or implies an official design system, the system's package is the direction (15.21) and this library only supplies the nearest slug so the anti-tells apply.

| The brief says | Slug |
|---|---|
| tool, console, ops, infrastructure, "exact", "engineered" | `industrial-precision` |
| magazine, essay, editorial, publication, "authored", "worth reading" | `editorial-print` |
| terminal, CLI, hacker, self-hosted, "no hand-holding" | `retro-terminal` |
| wellness, community, craft, "human", "warm", "handmade", "artisan" | `warm-organic` |
| poster, agency, launch, culture, "loud", "raw", "brutalist" | `brutalist-mono` |
| premium, luxury, hospitality, membership, "scarce", "quiet money" | `neo-luxury` |
| institution, university, documentation, "rational", "neutral", "Swiss" | `swiss-grid` |
| analytics, trading, observability, "dense", "power user" | `data-dense` |
| kids, games, habit, consumer mobile, "fun", "tactile", "pop" | `playful-pop` |
| notes, focus, reading, meditation, "calm", "minimal", "quiet" | `nordic-calm` |
| gaming, streaming, crypto, security, "neon", "kinetic", "cyber" | `cyber-neon` |
| consumer SaaS, settings, onboarding, "modern", "friendly", "layered" | `soft-depth` |

A vibe word the table does not carry maps to the nearest row by the "Reads as" column; a thirteenth direction is never invented to hold it. "Modern", "clean" and "premium" alone are not a read (15.20), so the one question goes to the user before this table is opened.

**Anti-tells** are the specific ways each direction gets built wrong. They matter more than the positive rules: every direction has a lazy version that reads as generic AI output, and the anti-tells name it so you can refuse it. The rotation rule (15.20) adds one anti-tell to every profile: the display face, the palette family and the hero architecture of the previous project.

Every font named below is freely licensed and installable today. Follow the substitute rule in 15.3: pair the named face with a stack ending in a generic family, and never cite a webfont URL.

| Slug | Canonical mode | Reads as |
|---|---|---|
| `industrial-precision` | dark | engineered, exact, unsentimental |
| `editorial-print` | light | authored, considered, worth reading |
| `retro-terminal` | dark | direct, expert, no hand-holding |
| `warm-organic` | light | human, unhurried, safe |
| `brutalist-mono` | light | honest, loud, uninterested in charming you |
| `neo-luxury` | dark | scarce, slow, expensive |
| `swiss-grid` | light | rational, ordered, neutral |
| `data-dense` | dark | professional, information-first, earned |
| `playful-pop` | light | energetic, tactile, low-stakes |
| `nordic-calm` | light | quiet, focused, unbothered |
| `cyber-neon` | dark | charged, kinetic, after-hours |
| `soft-depth` | light | modern, layered, approachable |

---

### Load rule (this is the point of the split)

The table above is the picker. **Read it, choose, then read only the chosen direction's profile**, sections 15.7 to 15.18. Reading all twelve profiles to use one is the waste this layout exists to remove.

| Situation | What to load |
|---|---|
| The user named a direction, or the project's `DESIGN.md` already sets one | that one profile |
| Choosing from scratch | this table, then the 2-3 profiles the table's "Reads as" column shortlists, then commit |
| Auditing an existing spec for conformance | the profile it claims, plus 15.3 |

Each profile carries the same nine blocks: Feels like, Palette logic, Surface & depth, Type pairing, Motion, **Dials** (the default `DESIGN_VARIANCE` / `MOTION_INTENSITY` / `VISUAL_DENSITY` triple, 15.20), Signature move, **Anti-tells**, and Best for.

### The profiles

| Direction | Profile |
|---|---|
| `industrial-precision` | 15.7 |
| `editorial-print` | 15.8 |
| `retro-terminal` | 15.9 |
| `warm-organic` | 15.10 |
| `brutalist-mono` | 15.11 |
| `neo-luxury` | 15.12 |
| `swiss-grid` | 15.13 |
| `data-dense` | 15.14 |
| `playful-pop` | 15.15 |
| `nordic-calm` | 15.16 |
| `cyber-neon` | 15.17 |
| `soft-depth` | 15.18 |

Twelve ready-to-drop full `DESIGN.md` specs, a different and deeper artifact, are section 17.
